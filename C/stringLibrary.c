#include<stdio.h>
#include<string.h>

void main(){
    
    // char name[] = "Arjit Kumar Sahoo";
    // printf("Length is = %d", strlen(name));


    char oldName[] = "oldname ";
    char newName[] = "newname";
    // strcpy(newName, oldName);
    // puts(newName);

    // strcat(oldName, newName);
    // puts(oldName);

    char firstStr[] = "Apple";
    char secStr[] = "Banana";
    printf("%d", strcmp(firstStr, secStr));

  
    // return 0;
}